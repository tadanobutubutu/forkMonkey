interface:
  display_name: "ForkMonkey"
  short_description: "Repo-specific patterns and workflows for forkMonkey"
  default_prompt: "Use the forkMonkey repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true